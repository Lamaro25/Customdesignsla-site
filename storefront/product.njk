<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-white text-gray-900">
  <header class="p-6 border-b border-gray-200">
    <a href="/" class="text-2xl font-bold">Custom Designs LA</a>
  </header>

  <main class="product-page container mx-auto px-4 py-10">
    <div class="grid md:grid-cols-2 gap-10">
      <!-- Product Images -->
      <div>
        {% if images %}
          {% for image in images %}
            <img src="{{ image.image | url }}" alt="{{ title }}" class="w-full mb-4 rounded-lg shadow">
          {% endfor %}
        {% else %}
          <p>No images uploaded yet.</p>
        {% endif %}
      </div>

      <!-- Product Details -->
      <div class="flex flex-col justify-center">
        <h1 class="text-3xl font-bold mb-4">{{ title }}</h1>

        {% if price %}
          <p class="text-xl font-semibold mb-2">$ {{ price | float }}</p>
        {% endif %}

        {% if material %}
          <p class="text-gray-700 mb-2">Material: {{ material }}</p>
        {% endif %}

        {% if size %}
          <p class="text-gray-700 mb-2">Size: {{ size | capitalize }}</p>
        {% endif %}

        {% if sku %}
          <p class="text-sm text-gray-500 mb-4">SKU: {{ sku }}</p>
        {% endif %}

        <div class="prose max-w-none mb-6">
          {{ content | safe }}
        </div>

        <button class="bg-black text-white px-6 py-3 rounded hover:bg-gray-800 transition">
          Buy Now (Coming Soon)
        </button>
      </div>
    </div>
  </main>

  <footer class="p-6 border-t border-gray-200 text-center text-sm">
    © {{ "now" | date: "%Y" }} Custom Designs LA
  </footer>
</body>
</html>
