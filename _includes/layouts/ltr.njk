{% extends "base.njk" %}

{% block head %}
  {{ super() }}
  <link rel="stylesheet" href="/static/css/ltr.css">
{% endblock %}

{% block content %}

  <!-- HERO SECTION (CSS Background Only — NO IMG TAG) -->
  <div class="ltr-hero">
    <h1>{{ title }}</h1>
    <p><em>{{ description }}</em></p>
  </div>

  <!-- COLLECTIONS SECTION BELOW HERO -->
  <section class="ltr-collections">

    <h2>Collections</h2>

    <!-- Western Button List -->
    <ul class="ltr-button-list">
      {{ content | safe }}
    </ul>

  </section>

{% endblock %}
