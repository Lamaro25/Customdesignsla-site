{% extends "layouts/base.njk" %}

{% block content %}
<section class="product-page">
  <div class="container">

    {# ===============================
       FEATURED PRODUCT IMAGE
       Uses images[0] ONLY (safe)
       =============================== #}
    {% if page.data.images and page.data.images.length %}
      <div class="product-featured-image">
        <img
          src="{{ page.data.images[0] | url }}"
          alt="{{ title }}"
          loading="eager"
        >
      </div>
    {% endif %}

    {# ===============================
       PRODUCT GALLERY (NON-DUPLICATED)
       Priority:
       1. gallery[] → Rings & Bracelets
       2. images[]  → Charms
       =============================== #}

    {% set displayImages = null %}

    {% if page.data.gallery and page.data.gallery.length %}
      {% set displayImages = page.data.gallery %}
    {% elseif page.data.images and page.data.images.length %}
      {% set displayImages = page.data
