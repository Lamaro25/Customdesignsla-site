<a href="{{ url }}" class="ltr-card">

  <!-- IMAGE SLOT -->
  <div class="ltr-card-image">
    {% if image %}
      <img src="{{ image }}" alt="{{ title }}">
    {% else %}
      <div class="ltr-placeholder">Image Coming Soon</div>
    {% endif %}
  </div>

  <!-- TEXT -->
  <div class="ltr-card-text">
    <h3 class="ltr-title">{{ title }}</h3>
    {% if price %}
      <p class="ltr-price">${{ price }} USD</p>
    {% endif %}
  </div>

</a>
